To install clone the repository.  Unzip the Windows installer and install.

Works on most Windows machines.

Also, you can run the game just from the python file, 
if you have The most current version of Python installed,
and the module pygame.